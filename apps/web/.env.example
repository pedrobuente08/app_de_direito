# Origem do NestJS (rewrite server-side — não expor em NEXT_PUBLIC_)
API_INTERNAL_URL=http://127.0.0.1:3001

# Prefixo usado pelo cliente (mesma origem → cookies httpOnly funcionam em dev)
NEXT_PUBLIC_API_BASE=/backend
