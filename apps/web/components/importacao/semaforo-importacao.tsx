'use client'

/** E5 — pré-visualização semafórica de importação PDF (stub). */
export function SemaforoImportacao() {
  return (
    <div className="mb-6 rounded-[var(--radius-md)] border border-dashed border-[var(--color-border-default)] bg-[var(--color-bg-subtle)] px-4 py-6 text-center text-sm text-[var(--color-text-secondary)]">
      Fluxo semafórico de PDFs (verde / amarelo / vermelho) — em implementação conforme PLANO_AJUSTES E5.
      Por enquanto continue usando o CSV abaixo para importação em lote.
    </div>
  )
}
